# Agent Shell Transcript

**Agent:** Auggie
**Started:** 2026-03-26 12:58:18
**Working Directory:** /Users/ldeck/tools/decknix/

---

## User (2026-03-26 12:58:18)

📂 Resumed session from 26/03/2026, 11:16:51 am: (1528 exchanges)


I think there's some opportunities for shortcuts to our process:
1. To review PRs
We currently open a new session (which involves choosing a workspace - which should default to the current workspace btw), name it, tag it, then run /review-service-pr and interact with the results.
I envisage we'll be able to turn various workflows into quick actions. e.g., `C-c A c r` to kick off a DWIM 'review' for the relevant content.
If the url provided points to a PR, then we should prompt (with defaults) to the workspace relevant, the name (default to pr-<projectId>-<prNumber>-<summary>), tags should include 'review', <projectId>, <author>, <jira>.
It should then automatically kick off performing a command 'review-service-pr' if for code review, or some other command tbd, or ask the user how to proceed which they can answer at the prompt.
2. With the above in place, we ought to be able to run `C-c A c B` to batch process a list of items. I imagine it could open a compose editor that would allow adding multiple items to be processed via different sessions. Some may be grouped together. So perhaps the editor could default to separating by newline, but if a line contains '---' only then treat the items above as a group. This way we could kick off multiple sessions simultaneously. WDYT? Actually we could group by starting a line with '---' meaning the subsequent lines are grouped until the next grouping or eof. We could optionally allow including the workspace dir in the grouping like '--- ~/Code/nurturecloud'. The default workspace would be the current workspace


## Agent (2026-03-26 12:58:25)



## Agent's Thoughts (2026-03-26 12:58:34)

The user is proposing two new features for the agent-shell system:

1. **Quick Actions** (`C-c A c r`) - A DWIM "review" action that:
   - Accepts a PR URL
   - Auto-detects workspace, name, tags from the PR metadata
   - Kicks off a review command automatically

2. **Batch Processing** (`C-c A c B`) - A compose editor for batch operations:
   - Multiple items separated by newlines
   - Grouping via `---` separator (optionally with workspace)
   - Kicks off multiple sessions simultaneously

This is a significant feature request. Before jumping into implementation, I should discuss the design, since the user explicitly asks "WDYT?" and there are several design decisions to make. Let me think through this and propose a plan rather than immediately coding.


I'm noticing the `C-c A c` prefix creates a new "commands" sub-namespace, and there are some tricky design questions here—particularly around URL parsing for GitHub PRs and how to handle multiple simultaneous sessions in batch mode. The `---` grouping syntax is clever but needs careful consideration for how workspaces interact with it.
## Agent (2026-03-26 12:58:34)

These are great ideas. Let me think through the design before we start building.

**On `C-c A c` as a "commands" prefix:**
That makes sense as a new sub-prefix. I'd suggest `C-c A c` for "commands/quick-actions" with:
- `C-c A c r` — review (DWIM from URL)
- `C-c A c B` — batch process

**On the review flow (`C-c A c r`):**

The flow I'm picturing:
1. Prompt for URL (default: kill-ring/clipboard if it looks like a PR URL)
2. Parse the URL → extract `owner/repo`, PR number
3. Prompt for workspace (default: current `project-root` or `default-directory`)
4. Auto-generate name: `pr-<repo>-<number>-<summary>` (summary from PR title via GitHub API, or just the number if offline)
5. Auto-generate tags: `["review", "<repo>", "<author>"]` — Jira key extraction from PR title/branch is possible but may need heuristics
6. Start session with those defaults, then auto-send `/review-service-pr <url>` as the first message

The tricky part is step 4 — fetching PR metadata. Options:
- **Lightweight**: Just parse the URL, skip API calls, use `pr-<repo>-<number>` as name
- **Rich**: Shell out to `gh pr view --json title,author,headRefName` for metadata. This is fast (~200ms) and gives us title, author, and branch name (which often contains the Jira key)

I'd lean toward the `gh` CLI approach since you already have it installed.

**On batch processing (`C-c A c B`):**

I like the `---` grouping syntax. Here's how I'd refine it:

```
--- ~/Code/nurturecloud
https://github.com/AcmeCorp/service-a/pull/123
https://github.com/AcmeCorp/service-b/pull/456

--- ~/Code/other-project
https://github.com/AcmeCorp/service-c/pull/789

https://github.com/AcmeCorp/standalone/pull/42
```

Rules:
- `---` starts a group. Optional path after `---` sets workspace for that group
- Lines without a preceding `---` use the default workspace (current buffer's)
- Empty lines are ignored
- Each URL within a group shares the same session (grouped review)
- Ungrouped URLs each get their own session
- Submit with `C-c C-c`, cancel with `C-c C-k`

One question: for grouped items, do you want them in a **single session** (one agent reviews multiple PRs together) or **parallel sessions** (one per URL, just sharing workspace)? I'd assume single session since grouping implies they're related.

**Implementation phases:**

1. **Phase 1**: `C-c A c r` — single PR review quick action (URL → session → auto-review)
2. **Phase 2**: `C-c A c B` — batch editor with `---` grouping and parallel session launch
3. **Phase 3**: Extensibility — other quick actions beyond review (e.g., `C-c A c i` for investigate-issue)

Shall I start with Phase 1? And do you want the `gh` CLI approach for PR metadata, or keep it lightweight (URL-parsing only)?


## User (2026-03-26 13:01:10)

Let's go



### Tool Call [completed]: Add 6 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:01:24
**Parameters:**
tasks: [((name . "Implement PR review quick action (C-c A c r)") (description . "Phase 1: URL → parse PR → fetch metadata via gh CLI → create session with auto-name/tags → send review command")) ((name . "Explore existing code for session creation and compose patterns") (description . "Understand current session-new flow, compose buffer, and keybinding setup")) ((name . "Implement URL parsing and gh CLI metadata fetch") (description . "Parse GitHub PR URLs, extract owner/repo/number, fetch title/author/branch via gh pr view --json")) ((name . "Implement the C-c A c r interactive command") (description . "Prompt for URL, workspace, then create session with auto-generated name/tags and send review command")) ((name . "Add C-c A c prefix keybinding with which-key labels") (description . "Set up the commands sub-prefix under C-c A")) ((name . "Build and test") (description . "nix build, commit, push"))]

```

```


### Tool Call [completed]: Update 2 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:01:29
**Parameters:**
tasks: [((task_id . "uDKFVJArj4d3Gus1DeRpjq") (state . "IN_PROGRESS")) ((task_id . "mJZfYQjw4Whe5TCmQqAPsq") (state . "IN_PROGRESS"))]

```
[/] Implement PR review quick action (C-c A c r)

[/] Explore existing code for session creation and compose patterns
```


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (search: "defun decknix-agent-session-new |C-c A|global-set-key.*agent|define-key.*agent-prefix|agent-prefix-map")

**Tool:** read
**Timestamp:** 2026-03-26 13:01:39
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
search_query_regex: defun decknix-agent-session-new |C-c A|global-set-key.*agent|define-key.*agent-prefix|agent-prefix-map
context_lines_before: 2
context_lines_after: 5

```

```


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (search: "review-service-pr|/review")

**Tool:** read
**Timestamp:** 2026-03-26 13:01:39
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
search_query_regex: review-service-pr|/review
context_lines_before: 2
context_lines_after: 5

```

```

## Agent (2026-03-26 13:01:43)

Now let me see the existing commands sub-prefix and the session-new flow more closely:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 2336-2430)

**Tool:** read
**Timestamp:** 2026-03-26 13:01:45
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [2336 2430]

```

```


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 1379-1445)

**Tool:** read
**Timestamp:** 2026-03-26 13:01:45
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [1379 1445]

```

```

## Agent (2026-03-26 13:01:50)

Now let me also check how the session sends its first message — I need to understand how to auto-send the review command after the session starts:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (search: "comint-send-input|shell-maker-submit|agent-shell-submit|process-send-string")

**Tool:** read
**Timestamp:** 2026-03-26 13:01:55
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
search_query_regex: comint-send-input|shell-maker-submit|agent-shell-submit|process-send-string
context_lines_before: 2
context_lines_after: 3

```

```


### Tool Call [completed]: Update 2 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:02:02
**Parameters:**
tasks: [((task_id . "mJZfYQjw4Whe5TCmQqAPsq") (state . "COMPLETE")) ((task_id . "653B6SjBaGfxoEcfdzqT3k") (state . "IN_PROGRESS"))]

```
[x] Explore existing code for session creation and compose patterns

[/] Implement URL parsing and gh CLI metadata fetch
```

## Agent (2026-03-26 13:02:07)

Good. I have the full picture now. Let me verify `gh` is available and test the metadata fetch:


### Tool Call [completed]: Run `which gh && gh --version | head -1
echo "==="
# Test fetching PR metadata (use a...`

**Tool:** execute
**Timestamp:** 2026-03-26 13:02:21
**Command:** which gh && gh --version | head -1
echo "==="
# Test fetching PR metadata (use a known public repo)
gh pr view --json title,author,headRefName,number --repo cli/cli 1 2>/dev/null | head -5 || echo "gh pr view test skipped"
**Parameters:**
wait: true
max_wait_seconds: 10
cwd: /Users/ldeck/tools/decknix

```

```

## Agent (2026-03-26 13:02:25)

`gh` works. Now let me implement the full feature. I'll add it right before the existing `C-c A c` keybinding block:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 2419-2430)

**Tool:** read
**Timestamp:** 2026-03-26 13:02:28
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [2419 2430]

```

```

## Agent (2026-03-26 13:02:31)

Now I'll add the PR review quick action functions before the keybinding block, and add the `r` key to the command map:


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:03:18
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:         ;; C-c A c — commands sub-prefix ("Commands")
        (define-prefix-command 'decknix-agent-command-map)
        (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
        (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
        (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
new_str_1:         ;; == Quick actions: PR review, batch processing ==
        ;; DWIM workflows that create a session with pre-configured name,
        ;; tags, workspace, and auto-send a command.

        (defun decknix--agent-parse-pr-url (url)
          "Parse a GitHub PR URL into an alist with owner, repo, number.
Returns nil if URL is not a valid GitHub PR URL.
Handles: https://github.com/OWNER/REPO/pull/NUMBER[/...]"
          (when (string-match
                 "github\\.com/\\([^/]+\\)/\\([^/]+\\)/pull/\\([0-9]+\\)"
                 url)
            (list (cons 'owner (match-string 1 url))
                  (cons 'repo (match-string 2 url))
                  (cons 'number (match-string 3 url)))))

        (defun decknix--agent-gh-pr-metadata (owner repo number)
          "Fetch PR metadata via gh CLI. Returns an alist or nil on failure.
Keys: title, author, branch, jira (extracted from branch name)."
          (condition-case nil
              (let* ((cmd (format "gh pr view %s --repo %s/%s --json title,author,headRefName"
                                  number owner repo))
                     (output (string-trim
                              (shell-command-to-string cmd))))
                (when (and output (not (string-empty-p output))
                           (string-prefix-p "{" output))
                  (let* ((json-object-type 'alist)
                         (json-array-type 'list)
                         (json-key-type 'symbol)
                         (data (json-read-from-string output))
                         (title (alist-get 'title data))
                         (author-obj (alist-get 'author data))
                         (author (when (listp author-obj)
                                   (alist-get 'login author-obj)))
                         (branch (alist-get 'headRefName data))
                         ;; Extract Jira key from branch: e.g. NC-1234-some-desc
                         (jira (when (and branch
                                         (string-match
                                          "\\([A-Z]+-[0-9]+\\)" branch))
                                 (match-string 1 branch)))
                         ;; Create a short summary from title (first 40 chars)
                         (summary (when title
                                    (let ((s (replace-regexp-in-string
                                              "[^a-zA-Z0-9]+" "-"
                                              (downcase (string-trim title)))))
                                      (if (> (length s) 40)
                                          (substring s 0 40)
                                        s)))))
                    (list (cons 'title title)
                          (cons 'author author)
                          (cons 'branch branch)
                          (cons 'jira jira)
                          (cons 'summary summary)))))
            (error nil)))

        (defun decknix--agent-clipboard-url ()
          "Return a GitHub PR URL from the kill ring or system clipboard, or nil."
          (let ((text (or (ignore-errors (current-kill 0 t))
                          (ignore-errors
                            (string-trim
                             (shell-command-to-string "pbpaste"))))))
            (when (and text (string-match-p "github\\.com/.*/pull/" text))
              (string-trim text))))

        (defvar decknix--review-pending nil
          "Pending review data: (BUF URL ATTEMPTS-LEFT).
Used by the retry timer to auto-send the review command once the
agent process is ready.")

        (defun decknix--agent-review-try-send ()
          "Try to send the review command to the pending session.
Retries if the process is not yet ready."
          (when decknix--review-pending
            (let ((buf (nth 0 decknix--review-pending))
                  (url (nth 1 decknix--review-pending))
                  (attempts-left (nth 2 decknix--review-pending)))
              (cond
               ((not (buffer-live-p buf))
                (setq decknix--review-pending nil))
               ((and (get-buffer-process buf)
                     (process-live-p (get-buffer-process buf)))
                ;; Process is ready — send the review command
                (with-current-buffer buf
                  (goto-char (point-max))
                  (shell-maker-submit
                   :input (format "/review-service-pr %s" url)))
                (setq decknix--review-pending nil)
                (message "Review started for %s" url))
               ((<= attempts-left 0)
                (setq decknix--review-pending nil)
                (message "Could not auto-send review: agent not ready"))
               (t
                (setf (nth 2 decknix--review-pending) (1- attempts-left))
                (run-at-time 2.0 nil #'decknix--agent-review-try-send))))))

        (defun decknix-agent-review-pr (url)
          "Start a PR review session for URL.
Parses the GitHub PR URL, fetches metadata via gh CLI, creates a
new session with auto-generated name and tags, then sends the
/review-service-pr command.

Interactively, prompts for URL (defaulting to clipboard if it
looks like a PR URL) and workspace (defaulting to current project)."
          (interactive
           (let* ((default-url (decknix--agent-clipboard-url))
                  (url (read-string
                        (if default-url
                            (format "PR URL [%s]: " default-url)
                          "PR URL: ")
                        nil nil default-url)))
             (list url)))
          ;; Parse and validate
          (let ((parsed (decknix--agent-parse-pr-url url)))
            (unless parsed
              (user-error "Not a valid GitHub PR URL: %s" url))
            (let* ((owner (alist-get 'owner parsed))
                   (repo (alist-get 'repo parsed))
                   (number (alist-get 'number parsed))
                   ;; Fetch metadata (non-blocking-ish, ~200ms)
                   (meta (progn
                           (message "Fetching PR #%s metadata…" number)
                           (decknix--agent-gh-pr-metadata owner repo number)))
                   (title (or (alist-get 'title meta) ""))
                   (author (alist-get 'author meta))
                   (jira (alist-get 'jira meta))
                   (summary (or (alist-get 'summary meta)
                                (format "%s" number)))
                   ;; Auto-generate session name: pr-<repo>-<number>-<summary>
                   (name (format "pr-%s-%s-%s" repo number
                                 (if (> (length summary) 30)
                                     (substring summary 0 30)
                                   summary)))
                   ;; Auto-generate tags
                   (tags (delq nil (list "review" repo author jira)))
                   ;; Workspace: default to current project root
                   (default-ws (decknix--agent-detect-workspace))
                   (workspace (read-directory-name
                               (format "Workspace for %s/%s#%s: "
                                       owner repo number)
                               default-ws nil t))
                   (workspace (expand-file-name workspace))
                   ;; Confirm name and tags
                   (name (read-string (format "Session name [%s]: " name)
                                      nil nil name))
                   (before-buffers (buffer-list))
                   ;; Build augmented command with --workspace-root
                   (augmented-cmd
                    (append agent-shell-auggie-acp-command
                            (list "--workspace-root" workspace)))
                   (config
                    (let ((base (agent-shell-auggie-make-agent-config)))
                      (setf (alist-get :client-maker base)
                            (eval `(lambda (buffer)
                                     (agent-shell--make-acp-client
                                      :command ,(car augmented-cmd)
                                      :command-params ',(cdr augmented-cmd)
                                      :environment-variables
                                      (cond ((map-elt agent-shell-auggie-authentication :none)
                                             agent-shell-auggie-environment)
                                            ((map-elt agent-shell-auggie-authentication :login)
                                             agent-shell-auggie-environment)
                                            (t
                                             (error "Invalid Auggie authentication")))
                                      :context-buffer buffer)) t))
                      base)))
              ;; Start the session
              (let ((default-directory workspace))
                (agent-shell-start :config config))
              (setq decknix--agent-session-cache-time 0)
              ;; Post-creation: rename, tags, workspace, then auto-send review
              (decknix--agent-session-new-post-create
               before-buffers name tags workspace)
              ;; Schedule auto-sending the review command once the process is ready.
              ;; We need to find the new buffer first (same pattern as post-create).
              (let ((pr-url url)
                    (bufs before-buffers))
                (run-at-time
                 3.0 nil
                 (eval
                  `(lambda ()
                     (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
                       (when shell-buf
                         (setq decknix--review-pending
                               (list shell-buf ,pr-url 5))
                         (decknix--agent-review-try-send))))
                  t)))
              (message "Starting review: %s/%s#%s — %s" owner repo number title))))

        ;; C-c A c — commands sub-prefix ("Commands")
        (define-prefix-command 'decknix-agent-command-map)
        (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
        (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
        (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
old_str_start_line_number_1: 2421
old_str_end_line_number_1: 2426

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1,6 +1,194 @@
+        ;; == Quick actions: PR review, batch processing ==
+        ;; DWIM workflows that create a session with pre-configured name,
+        ;; tags, workspace, and auto-send a command.
+
+        (defun decknix--agent-parse-pr-url (url)
+          "Parse a GitHub PR URL into an alist with owner, repo, number.
+Returns nil if URL is not a valid GitHub PR URL.
+Handles: https://github.com/OWNER/REPO/pull/NUMBER[/...]"
+          (when (string-match
+                 "github\\.com/\\([^/]+\\)/\\([^/]+\\)/pull/\\([0-9]+\\)"
+                 url)
+            (list (cons 'owner (match-string 1 url))
+                  (cons 'repo (match-string 2 url))
+                  (cons 'number (match-string 3 url)))))
+
+        (defun decknix--agent-gh-pr-metadata (owner repo number)
+          "Fetch PR metadata via gh CLI. Returns an alist or nil on failure.
+Keys: title, author, branch, jira (extracted from branch name)."
+          (condition-case nil
+              (let* ((cmd (format "gh pr view %s --repo %s/%s --json title,author,headRefName"
+                                  number owner repo))
+                     (output (string-trim
+                              (shell-command-to-string cmd))))
+                (when (and output (not (string-empty-p output))
+                           (string-prefix-p "{" output))
+                  (let* ((json-object-type 'alist)
+                         (json-array-type 'list)
+                         (json-key-type 'symbol)
+                         (data (json-read-from-string output))
+                         (title (alist-get 'title data))
+                         (author-obj (alist-get 'author data))
+                         (author (when (listp author-obj)
+                                   (alist-get 'login author-obj)))
+                         (branch (alist-get 'headRefName data))
+                         ;; Extract Jira key from branch: e.g. NC-1234-some-desc
+                         (jira (when (and branch
+                                         (string-match
+                                          "\\([A-Z]+-[0-9]+\\)" branch))
+                                 (match-string 1 branch)))
+                         ;; Create a short summary from title (first 40 chars)
+                         (summary (when title
+                                    (let ((s (replace-regexp-in-string
+                                              "[^a-zA-Z0-9]+" "-"
+                                              (downcase (string-trim title)))))
+                                      (if (> (length s) 40)
+                                          (substring s 0 40)
+                                        s)))))
+                    (list (cons 'title title)
+                          (cons 'author author)
+                          (cons 'branch branch)
+                          (cons 'jira jira)
+                          (cons 'summary summary)))))
+            (error nil)))
+
+        (defun decknix--agent-clipboard-url ()
+          "Return a GitHub PR URL from the kill ring or system clipboard, or nil."
+          (let ((text (or (ignore-errors (current-kill 0 t))
+                          (ignore-errors
+                            (string-trim
+                             (shell-command-to-string "pbpaste"))))))
+            (when (and text (string-match-p "github\\.com/.*/pull/" text))
+              (string-trim text))))
+
+        (defvar decknix--review-pending nil
+          "Pending review data: (BUF URL ATTEMPTS-LEFT).
+Used by the retry timer to auto-send the review command once the
+agent process is ready.")
+
+        (defun decknix--agent-review-try-send ()
+          "Try to send the review command to the pending session.
+Retries if the process is not yet ready."
+          (when decknix--review-pending
+            (let ((buf (nth 0 decknix--review-pending))
+                  (url (nth 1 decknix--review-pending))
+                  (attempts-left (nth 2 decknix--review-pending)))
+              (cond
+               ((not (buffer-live-p buf))
+                (setq decknix--review-pending nil))
+               ((and (get-buffer-process buf)
+                     (process-live-p (get-buffer-process buf)))
+                ;; Process is ready — send the review command
+                (with-current-buffer buf
+                  (goto-char (point-max))
+                  (shell-maker-submit
+                   :input (format "/review-service-pr %s" url)))
+                (setq decknix--review-pending nil)
+                (message "Review started for %s" url))
+               ((<= attempts-left 0)
+                (setq decknix--review-pending nil)
+                (message "Could not auto-send review: agent not ready"))
+               (t
+                (setf (nth 2 decknix--review-pending) (1- attempts-left))
+                (run-at-time 2.0 nil #'decknix--agent-review-try-send))))))
+
+        (defun decknix-agent-review-pr (url)
+          "Start a PR review session for URL.
+Parses the GitHub PR URL, fetches metadata via gh CLI, creates a
+new session with auto-generated name and tags, then sends the
+/review-service-pr command.
+
+Interactively, prompts for URL (defaulting to clipboard if it
+looks like a PR URL) and workspace (defaulting to current project)."
+          (interactive
+           (let* ((default-url (decknix--agent-clipboard-url))
+                  (url (read-string
+                        (if default-url
+                            (format "PR URL [%s]: " default-url)
+                          "PR URL: ")
+                        nil nil default-url)))
+             (list url)))
+          ;; Parse and validate
+          (let ((parsed (decknix--agent-parse-pr-url url)))
+            (unless parsed
+              (user-error "Not a valid GitHub PR URL: %s" url))
+            (let* ((owner (alist-get 'owner parsed))
+                   (repo (alist-get 'repo parsed))
+                   (number (alist-get 'number parsed))
+                   ;; Fetch metadata (non-blocking-ish, ~200ms)
+                   (meta (progn
+                           (message "Fetching PR #%s metadata…" number)
+                           (decknix--agent-gh-pr-metadata owner repo number)))
+                   (title (or (alist-get 'title meta) ""))
+                   (author (alist-get 'author meta))
+                   (jira (alist-get 'jira meta))
+                   (summary (or (alist-get 'summary meta)
+                                (format "%s" number)))
+                   ;; Auto-generate session name: pr-<repo>-<number>-<summary>
+                   (name (format "pr-%s-%s-%s" repo number
+                                 (if (> (length summary) 30)
+                                     (substring summary 0 30)
+                                   summary)))
+                   ;; Auto-generate tags
+                   (tags (delq nil (list "review" repo author jira)))
+                   ;; Workspace: default to current project root
+                   (default-ws (decknix--agent-detect-workspace))
+                   (workspace (read-directory-name
+                               (format "Workspace for %s/%s#%s: "
+                                       owner repo number)
+                               default-ws nil t))
+                   (workspace (expand-file-name workspace))
+                   ;; Confirm name and tags
+                   (name (read-string (format "Session name [%s]: " name)
+                                      nil nil name))
+                   (before-buffers (buffer-list))
+                   ;; Build augmented command with --workspace-root
+                   (augmented-cmd
+                    (append agent-shell-auggie-acp-command
+                            (list "--workspace-root" workspace)))
+                   (config
+                    (let ((base (agent-shell-auggie-make-agent-config)))
+                      (setf (alist-get :client-maker base)
+                            (eval `(lambda (buffer)
+                                     (agent-shell--make-acp-client
+                                      :command ,(car augmented-cmd)
+                                      :command-params ',(cdr augmented-cmd)
+                                      :environment-variables
+                                      (cond ((map-elt agent-shell-auggie-authentication :none)
+                                             agent-shell-auggie-environment)
+                                            ((map-elt agent-shell-auggie-authentication :login)
+                                             agent-shell-auggie-environment)
+                                            (t
+                                             (error "Invalid Auggie authentication")))
+                                      :context-buffer buffer)) t))
+                      base)))
+              ;; Start the session
+              (let ((default-directory workspace))
+                (agent-shell-start :config config))
+              (setq decknix--agent-session-cache-time 0)
+              ;; Post-creation: rename, tags, workspace, then auto-send review
+              (decknix--agent-session-new-post-create
+               before-buffers name tags workspace)
+              ;; Schedule auto-sending the review command once the process is ready.
+              ;; We need to find the new buffer first (same pattern as post-create).
+              (let ((pr-url url)
+                    (bufs before-buffers))
+                (run-at-time
+                 3.0 nil
+                 (eval
+                  `(lambda ()
+                     (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
+                       (when shell-buf
+                         (setq decknix--review-pending
+                               (list shell-buf ,pr-url 5))
+                         (decknix--agent-review-try-send))))
+                  t)))
+              (message "Starting review: %s/%s#%s — %s" owner repo number title))))
+
         ;; C-c A c — commands sub-prefix ("Commands")
         (define-prefix-command 'decknix-agent-command-map)
         (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
         (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
         (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
-        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
\ No newline at end of file
+        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
+        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
\ No newline at end of file
```

## Agent (2026-03-26 13:03:24)

Now add which-key labels for the new `r` key:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (search: ""C-c A c".*"Commands"")

**Tool:** read
**Timestamp:** 2026-03-26 13:03:26
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
search_query_regex: "C-c A c".*"Commands"
context_lines_before: 2
context_lines_after: 5

```

```


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:03:33
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:             "C-c A c" "Commands"
new_str_1:             "C-c A c" "Commands"
            "C-c A c r" "review PR"
            "C-c A c c" "run command"
            "C-c A c n" "new command"
            "C-c A c e" "edit command"
old_str_start_line_number_1: 624
old_str_end_line_number_1: 624

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1 +1,5 @@
-            "C-c A c" "Commands"
\ No newline at end of file
+            "C-c A c" "Commands"
+            "C-c A c r" "review PR"
+            "C-c A c c" "run command"
+            "C-c A c n" "new command"
+            "C-c A c e" "edit command"
\ No newline at end of file
```

## Agent (2026-03-26 13:03:38)

Now update the help/keybinding reference to include the new command:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (search: ""  C-c A n.*Force new|Global.*C-c A")

**Tool:** read
**Timestamp:** 2026-03-26 13:03:40
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
search_query_regex: "  C-c A n.*Force new|Global.*C-c A
context_lines_before: 1
context_lines_after: 10

```

```


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:03:51
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:             "  C-c A a     Start / switch to agent\n"
            "  C-c A n     Force new session\n"
            "  C-c A s     Session picker\n"
            "  C-c A h     View history (C-u to pick)\n"
            "  C-c A e     Compose buffer\n"
            "  C-c A ? k   This keybinding reference\n"
new_str_1:             "  C-c A a     Start / switch to agent\n"
            "  C-c A n     Force new session\n"
            "  C-c A s     Session picker\n"
            "  C-c A h     View history (C-u to pick)\n"
            "  C-c A e     Compose buffer\n"
            "  C-c A c r   Review PR (quick action)\n"
            "  C-c A ? k   This keybinding reference\n"
old_str_start_line_number_1: 473
old_str_end_line_number_1: 478

```
╭─────────╮
│ changes │
╰─────────╯

@@ -3,4 +3,5 @@
             "  C-c A s     Session picker\n"
             "  C-c A h     View history (C-u to pick)\n"
             "  C-c A e     Compose buffer\n"
+            "  C-c A c r   Review PR (quick action)\n"
             "  C-c A ? k   This keybinding reference\n"
\ No newline at end of file
```


### Tool Call [completed]: Update 4 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:04:01
**Parameters:**
tasks: [((task_id . "653B6SjBaGfxoEcfdzqT3k") (state . "COMPLETE")) ((task_id . "shnKkgXHoiTxtqbG2KKSZm") (state . "COMPLETE")) ((task_id . "gCpSXuMnbLqXrnjYadsvtx") (state . "COMPLETE")) ((task_id . "x293ZaG1WqhJJkL24T8MfN") (state . "IN_PROGRESS"))]

```
[x] Implement URL parsing and gh CLI metadata fetch

[x] Implement the C-c A c r interactive command

[x] Add C-c A c prefix keybinding with which-key labels

[/] Build and test
```

## Agent (2026-03-26 13:04:05)

Now build:


### Tool Call [completed]: Run `cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure...`

**Tool:** execute
**Timestamp:** 2026-03-26 13:04:47
**Command:** cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure --override-input decknix path:$HOME/tools/decknix 2>&1 | tail -15
**Parameters:**
wait: true
max_wait_seconds: 180
cwd: /Users/ldeck

```

```

## Agent (2026-03-26 13:04:58)

Build succeeded. Let me commit and push:


### Tool Call [completed]: Run `cd ~/tools/decknix && git add -A && git diff --cached --stat`

**Tool:** execute
**Timestamp:** 2026-03-26 13:05:00
**Command:** cd ~/tools/decknix && git add -A && git diff --cached --stat
**Parameters:**
wait: true
max_wait_seconds: 5
cwd: /Users/ldeck/tools/decknix

```

```
## User (2026-03-26 13:05:42)

See my last question on discussing further?


## Agent (2026-03-26 13:06:02)


## Agent's Thoughts (2026-03-26 13:06:02)

The user is referring to their original message where they asked "WDYT?" about the batch processing feature and the grouping syntax. Let me re-read their proposal more carefully and respond to the discussion points they raised, rather than just jumping into implementation.

They asked about:
1. The `C-c A c r` review flow - which I've now implemented
2. The `C-c A c B` batch processing flow - which we discussed but haven't built yet

But they're asking about something else - "See my last question on discussing further?" - let me re-read their original message...

They said "WDYT?" about the batch processing concept with the `---` grouping syntax. They also mentioned that the workspace should default to the current workspace when creating new sessions.


Looking back at their message more carefully, I see they wanted to discuss several design points: the workspace defaulting to the current one when creating new sessions via `C-c A n`, the batch processing concept with `---` grouping, and the overall quick actions framework. They specifically asked "WDYT?" about the batch processing idea, I shared my thoughts, and they said "Let's go" - so I started implementing. But now they're asking if I want to discuss further, which suggests I may have jumped straight into coding without fully exploring the design implications first.
## Agent (2026-03-26 13:06:02)

Right — you raised several points I glossed over when I jumped into coding:

1. **`C-c A n` workspace default** — you noted "which should default to the current workspace btw". Currently `decknix-agent-session-new` does use `decknix--agent-detect-workspace` which calls `project-root` or falls back to `default-directory`. If you're invoking from an agent-shell buffer, `default-directory` might be `~/` rather than the project you're working in. Should I fix that to prefer the *calling buffer's* project root (e.g., the buffer you were in before switching to the agent)?

2. **Batch processing (`C-c A c B`)** — your refined grouping syntax:
   ```
   --- ~/Code/nurturecloud
   https://github.com/org/service-a/pull/123
   https://github.com/org/service-b/pull/456
   
   --- ~/Code/other-project
   https://github.com/org/service-c/pull/789
   
   https://github.com/org/standalone/pull/42
   ```
   - `---` starts a group, optional workspace after it
   - Lines within a group → single session (agent reviews multiple PRs together)
   - Ungrouped lines → individual sessions each
   - Default workspace = current workspace

   **My questions back:**
   - Should grouped PRs get a combined name like `review-batch-nurturecloud-2prs` or individual names?
   - For the compose editor — should it be the same compose buffer mechanism we already have, or a dedicated batch buffer with its own mode (syntax highlighting for `---`, validation before submit)?
   - How should progress be shown? A message per session launched? Or a summary buffer tracking all of them?

3. **Extensibility beyond review** — you mentioned "various workflows". Should `C-c A c r` be the first of a pattern where each quick action is a function that knows how to parse a URL/input, derive metadata, and auto-send a command? That way adding `C-c A c i` (investigate issue) later just follows the same template.

What are your thoughts on these before I continue?


## User (2026-03-26 13:14:05)

Perhaps grouped prs can be named via their divider. e.g., --- <name> [: <workspace>]
The compose editor we have works pretty well, but certainly some syntax highlighting would be great. Perhaps that's a minor mode.
A summary buffer would be great. This might be the bare beginnings of our dashboard.
Extensibility sounds reasonable as suggested.

For speed of initiation atm (unless it proves insufficient), I'm thinking we might actually use the simple parsing of a url provided for now instead of requiring gh etc 
Those kinds of things, where applicable, could happen after a session is kicked off and be part of the 'command' it runs, allowing post action tagging etc to occur.



### Tool Call [completed]: Update 1 task

**Tool:** edit
**Timestamp:** 2026-03-26 13:14:19
**Parameters:**
tasks: [((task_id . "x293ZaG1WqhJJkL24T8MfN") (state . "IN_PROGRESS") (name . "Simplify PR review to URL-only parsing (no gh CLI)") (description . "Remove gh CLI metadata fetch, use URL-only parsing for fast initiation. Tags/metadata can be enriched post-session."))]

```
[/] Simplify PR review to URL-only parsing (no gh CLI)
```


### Tool Call [completed]: Add 3 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:14:19
**Parameters:**
tasks: [((name . "Implement batch compose editor (C-c A c B)") (description . "Compose buffer with --- <name> [: <workspace>] grouping syntax. Each group becomes a session. Ungrouped lines get individual sessions.")) ((name . "Add batch-review minor mode for syntax highlighting") (description . "Minor mode for the batch compose buffer: highlight --- dividers, URLs, workspace paths")) ((name . "Implement batch launch summary buffer") (description . "Dashboard-style buffer showing launched sessions and their status (beginning of session dashboard)"))]

```

```

## Agent (2026-03-26 13:14:25)

Good direction. Let me simplify the PR review to URL-only parsing — strip out the `gh` CLI call and make it instant. The command itself (`/review-service-pr`) can handle metadata enrichment and post-session tagging.


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 2421-2620)

**Tool:** read
**Timestamp:** 2026-03-26 13:14:28
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [2421 2620]

```

```

## Agent (2026-03-26 13:14:34)

Now I'll replace the entire quick actions section — remove `gh` CLI metadata, simplify to URL-only parsing, and make it instant:


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:15:33
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:         ;; == Quick actions: PR review, batch processing ==
        ;; DWIM workflows that create a session with pre-configured name,
        ;; tags, workspace, and auto-send a command.

        (defun decknix--agent-parse-pr-url (url)
          "Parse a GitHub PR URL into an alist with owner, repo, number.
Returns nil if URL is not a valid GitHub PR URL.
Handles: https://github.com/OWNER/REPO/pull/NUMBER[/...]"
          (when (string-match
                 "github\\.com/\\([^/]+\\)/\\([^/]+\\)/pull/\\([0-9]+\\)"
                 url)
            (list (cons 'owner (match-string 1 url))
                  (cons 'repo (match-string 2 url))
                  (cons 'number (match-string 3 url)))))

        (defun decknix--agent-gh-pr-metadata (owner repo number)
          "Fetch PR metadata via gh CLI. Returns an alist or nil on failure.
Keys: title, author, branch, jira (extracted from branch name)."
          (condition-case nil
              (let* ((cmd (format "gh pr view %s --repo %s/%s --json title,author,headRefName"
                                  number owner repo))
                     (output (string-trim
                              (shell-command-to-string cmd))))
                (when (and output (not (string-empty-p output))
                           (string-prefix-p "{" output))
                  (let* ((json-object-type 'alist)
                         (json-array-type 'list)
                         (json-key-type 'symbol)
                         (data (json-read-from-string output))
                         (title (alist-get 'title data))
                         (author-obj (alist-get 'author data))
                         (author (when (listp author-obj)
                                   (alist-get 'login author-obj)))
                         (branch (alist-get 'headRefName data))
                         ;; Extract Jira key from branch: e.g. NC-1234-some-desc
                         (jira (when (and branch
                                         (string-match
                                          "\\([A-Z]+-[0-9]+\\)" branch))
                                 (match-string 1 branch)))
                         ;; Create a short summary from title (first 40 chars)
                         (summary (when title
                                    (let ((s (replace-regexp-in-string
                                              "[^a-zA-Z0-9]+" "-"
                                              (downcase (string-trim title)))))
                                      (if (> (length s) 40)
                                          (substring s 0 40)
                                        s)))))
                    (list (cons 'title title)
                          (cons 'author author)
                          (cons 'branch branch)
                          (cons 'jira jira)
                          (cons 'summary summary)))))
            (error nil)))

        (defun decknix--agent-clipboard-url ()
          "Return a GitHub PR URL from the kill ring or system clipboard, or nil."
          (let ((text (or (ignore-errors (current-kill 0 t))
                          (ignore-errors
                            (string-trim
                             (shell-command-to-string "pbpaste"))))))
            (when (and text (string-match-p "github\\.com/.*/pull/" text))
              (string-trim text))))

        (defvar decknix--review-pending nil
          "Pending review data: (BUF URL ATTEMPTS-LEFT).
Used by the retry timer to auto-send the review command once the
agent process is ready.")

        (defun decknix--agent-review-try-send ()
          "Try to send the review command to the pending session.
Retries if the process is not yet ready."
          (when decknix--review-pending
            (let ((buf (nth 0 decknix--review-pending))
                  (url (nth 1 decknix--review-pending))
                  (attempts-left (nth 2 decknix--review-pending)))
              (cond
               ((not (buffer-live-p buf))
                (setq decknix--review-pending nil))
               ((and (get-buffer-process buf)
                     (process-live-p (get-buffer-process buf)))
                ;; Process is ready — send the review command
                (with-current-buffer buf
                  (goto-char (point-max))
                  (shell-maker-submit
                   :input (format "/review-service-pr %s" url)))
                (setq decknix--review-pending nil)
                (message "Review started for %s" url))
               ((<= attempts-left 0)
                (setq decknix--review-pending nil)
                (message "Could not auto-send review: agent not ready"))
               (t
                (setf (nth 2 decknix--review-pending) (1- attempts-left))
                (run-at-time 2.0 nil #'decknix--agent-review-try-send))))))

        (defun decknix-agent-review-pr (url)
          "Start a PR review session for URL.
Parses the GitHub PR URL, fetches metadata via gh CLI, creates a
new session with auto-generated name and tags, then sends the
/review-service-pr command.

Interactively, prompts for URL (defaulting to clipboard if it
looks like a PR URL) and workspace (defaulting to current project)."
          (interactive
           (let* ((default-url (decknix--agent-clipboard-url))
                  (url (read-string
                        (if default-url
                            (format "PR URL [%s]: " default-url)
                          "PR URL: ")
                        nil nil default-url)))
             (list url)))
          ;; Parse and validate
          (let ((parsed (decknix--agent-parse-pr-url url)))
            (unless parsed
              (user-error "Not a valid GitHub PR URL: %s" url))
            (let* ((owner (alist-get 'owner parsed))
                   (repo (alist-get 'repo parsed))
                   (number (alist-get 'number parsed))
                   ;; Fetch metadata (non-blocking-ish, ~200ms)
                   (meta (progn
                           (message "Fetching PR #%s metadata…" number)
                           (decknix--agent-gh-pr-metadata owner repo number)))
                   (title (or (alist-get 'title meta) ""))
                   (author (alist-get 'author meta))
                   (jira (alist-get 'jira meta))
                   (summary (or (alist-get 'summary meta)
                                (format "%s" number)))
                   ;; Auto-generate session name: pr-<repo>-<number>-<summary>
                   (name (format "pr-%s-%s-%s" repo number
                                 (if (> (length summary) 30)
                                     (substring summary 0 30)
                                   summary)))
                   ;; Auto-generate tags
                   (tags (delq nil (list "review" repo author jira)))
                   ;; Workspace: default to current project root
                   (default-ws (decknix--agent-detect-workspace))
                   (workspace (read-directory-name
                               (format "Workspace for %s/%s#%s: "
                                       owner repo number)
                               default-ws nil t))
                   (workspace (expand-file-name workspace))
                   ;; Confirm name and tags
                   (name (read-string (format "Session name [%s]: " name)
                                      nil nil name))
                   (before-buffers (buffer-list))
                   ;; Build augmented command with --workspace-root
                   (augmented-cmd
                    (append agent-shell-auggie-acp-command
                            (list "--workspace-root" workspace)))
                   (config
                    (let ((base (agent-shell-auggie-make-agent-config)))
                      (setf (alist-get :client-maker base)
                            (eval `(lambda (buffer)
                                     (agent-shell--make-acp-client
                                      :command ,(car augmented-cmd)
                                      :command-params ',(cdr augmented-cmd)
                                      :environment-variables
                                      (cond ((map-elt agent-shell-auggie-authentication :none)
                                             agent-shell-auggie-environment)
                                            ((map-elt agent-shell-auggie-authentication :login)
                                             agent-shell-auggie-environment)
                                            (t
                                             (error "Invalid Auggie authentication")))
                                      :context-buffer buffer)) t))
                      base)))
              ;; Start the session
              (let ((default-directory workspace))
                (agent-shell-start :config config))
              (setq decknix--agent-session-cache-time 0)
              ;; Post-creation: rename, tags, workspace, then auto-send review
              (decknix--agent-session-new-post-create
               before-buffers name tags workspace)
              ;; Schedule auto-sending the review command once the process is ready.
              ;; We need to find the new buffer first (same pattern as post-create).
              (let ((pr-url url)
                    (bufs before-buffers))
                (run-at-time
                 3.0 nil
                 (eval
                  `(lambda ()
                     (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
                       (when shell-buf
                         (setq decknix--review-pending
                               (list shell-buf ,pr-url 5))
                         (decknix--agent-review-try-send))))
                  t)))
              (message "Starting review: %s/%s#%s — %s" owner repo number title))))
new_str_1:         ;; == Quick actions: PR review, batch processing ==
        ;; DWIM workflows that create a session with pre-configured name,
        ;; tags, workspace, and auto-send a command.
        ;; Metadata enrichment (author, Jira key, etc.) is deferred to the
        ;; review command itself, keeping initiation instant.

        (defun decknix--agent-parse-pr-url (url)
          "Parse a GitHub PR URL into an alist with owner, repo, number.
Returns nil if URL is not a valid GitHub PR URL.
Handles: https://github.com/OWNER/REPO/pull/NUMBER[/...]"
          (when (string-match
                 "github\\.com/\\([^/]+\\)/\\([^/]+\\)/pull/\\([0-9]+\\)"
                 url)
            (list (cons 'owner (match-string 1 url))
                  (cons 'repo (match-string 2 url))
                  (cons 'number (match-string 3 url)))))

        (defun decknix--agent-clipboard-url ()
          "Return a GitHub PR URL from the kill ring or system clipboard, or nil."
          (let ((text (or (ignore-errors (current-kill 0 t))
                          (ignore-errors
                            (string-trim
                             (shell-command-to-string "pbpaste"))))))
            (when (and text (string-match-p "github\\.com/.*/pull/" text))
              (string-trim text))))

        (defvar decknix--quickaction-pending nil
          "Pending quick-action data: (BUF COMMAND ATTEMPTS-LEFT).
Used by the retry timer to auto-send a command once the agent
process is ready.")

        (defun decknix--agent-quickaction-try-send ()
          "Try to send the pending command to the session buffer.
Retries if the process is not yet ready."
          (when decknix--quickaction-pending
            (let ((buf (nth 0 decknix--quickaction-pending))
                  (command (nth 1 decknix--quickaction-pending))
                  (attempts-left (nth 2 decknix--quickaction-pending)))
              (cond
               ((not (buffer-live-p buf))
                (setq decknix--quickaction-pending nil))
               ((and (get-buffer-process buf)
                     (process-live-p (get-buffer-process buf)))
                ;; Process is ready — send the command
                (with-current-buffer buf
                  (goto-char (point-max))
                  (shell-maker-submit :input command))
                (setq decknix--quickaction-pending nil)
                (message "Sent: %s" (truncate-string-to-width command 60)))
               ((<= attempts-left 0)
                (setq decknix--quickaction-pending nil)
                (message "Could not auto-send command: agent not ready"))
               (t
                (setf (nth 2 decknix--quickaction-pending) (1- attempts-left))
                (run-at-time 2.0 nil #'decknix--agent-quickaction-try-send))))))

        (defun decknix--agent-quickaction-start (name tags workspace command)
          "Start a quick-action session with NAME, TAGS, WORKSPACE, and auto-send COMMAND.
Creates a new agent session, applies metadata, then schedules sending
COMMAND once the process is ready.  Returns immediately."
          (let* ((workspace (expand-file-name workspace))
                 (before-buffers (buffer-list))
                 (augmented-cmd
                  (append agent-shell-auggie-acp-command
                          (list "--workspace-root" workspace)))
                 (config
                  (let ((base (agent-shell-auggie-make-agent-config)))
                    (setf (alist-get :client-maker base)
                          (eval `(lambda (buffer)
                                   (agent-shell--make-acp-client
                                    :command ,(car augmented-cmd)
                                    :command-params ',(cdr augmented-cmd)
                                    :environment-variables
                                    (cond ((map-elt agent-shell-auggie-authentication :none)
                                           agent-shell-auggie-environment)
                                          ((map-elt agent-shell-auggie-authentication :login)
                                           agent-shell-auggie-environment)
                                          (t
                                           (error "Invalid Auggie authentication")))
                                    :context-buffer buffer)) t))
                    base)))
            (let ((default-directory workspace))
              (agent-shell-start :config config))
            (setq decknix--agent-session-cache-time 0)
            (decknix--agent-session-new-post-create
             before-buffers name tags workspace)
            ;; Schedule auto-sending the command once the process is ready.
            (let ((cmd command)
                  (bufs before-buffers))
              (run-at-time
               3.0 nil
               (eval
                `(lambda ()
                   (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
                     (when shell-buf
                       (setq decknix--quickaction-pending
                             (list shell-buf ,cmd 5))
                       (decknix--agent-quickaction-try-send))))
                t)))))

        (defun decknix-agent-review-pr (url)
          "Start a PR review session for URL.
Parses the GitHub PR URL, creates a new session with auto-generated
name and tags, then sends /review-service-pr.  Metadata enrichment
\(author, Jira key, title\) is handled by the review command itself.

Interactively, prompts for URL (defaulting to clipboard if it
looks like a PR URL) and workspace (defaulting to current project)."
          (interactive
           (let* ((default-url (decknix--agent-clipboard-url))
                  (url (read-string
                        (if default-url
                            (format "PR URL [%s]: " default-url)
                          "PR URL: ")
                        nil nil default-url)))
             (list url)))
          ;; Parse and validate
          (let ((parsed (decknix--agent-parse-pr-url url)))
            (unless parsed
              (user-error "Not a valid GitHub PR URL: %s" url))
            (let* ((owner (alist-get 'owner parsed))
                   (repo (alist-get 'repo parsed))
                   (number (alist-get 'number parsed))
                   ;; Auto-generate session name: pr-<repo>-<number>
                   (name (format "pr-%s-%s" repo number))
                   ;; Tags from URL only — command can enrich later
                   (tags (list "review" repo))
                   ;; Workspace: default to current project root
                   (default-ws (decknix--agent-detect-workspace))
                   (workspace (read-directory-name
                               (format "Workspace for %s/%s#%s: "
                                       owner repo number)
                               default-ws nil t))
                   ;; Confirm name
                   (name (read-string (format "Session name [%s]: " name)
                                      nil nil name))
                   (command (format "/review-service-pr %s" url)))
              (decknix--agent-quickaction-start name tags workspace command)
              (message "Starting review: %s/%s#%s" owner repo number))))
old_str_start_line_number_1: 2426
old_str_end_line_number_1: 2611

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1,6 +1,8 @@
         ;; == Quick actions: PR review, batch processing ==
         ;; DWIM workflows that create a session with pre-configured name,
         ;; tags, workspace, and auto-send a command.
+        ;; Metadata enrichment (author, Jira key, etc.) is deferred to the
+        ;; review command itself, keeping initiation instant.
 
         (defun decknix--agent-parse-pr-url (url)
           "Parse a GitHub PR URL into an alist with owner, repo, number.
@@ -13,45 +15,6 @@
                   (cons 'repo (match-string 2 url))
                   (cons 'number (match-string 3 url)))))
 
-        (defun decknix--agent-gh-pr-metadata (owner repo number)
-          "Fetch PR metadata via gh CLI. Returns an alist or nil on failure.
-Keys: title, author, branch, jira (extracted from branch name)."
-          (condition-case nil
-              (let* ((cmd (format "gh pr view %s --repo %s/%s --json title,author,headRefName"
-                                  number owner repo))
-                     (output (string-trim
-                              (shell-command-to-string cmd))))
-                (when (and output (not (string-empty-p output))
-                           (string-prefix-p "{" output))
-                  (let* ((json-object-type 'alist)
-                         (json-array-type 'list)
-                         (json-key-type 'symbol)
-                         (data (json-read-from-string output))
-                         (title (alist-get 'title data))
-                         (author-obj (alist-get 'author data))
-                         (author (when (listp author-obj)
-                                   (alist-get 'login author-obj)))
-                         (branch (alist-get 'headRefName data))
-                         ;; Extract Jira key from branch: e.g. NC-1234-some-desc
-                         (jira (when (and branch
-                                         (string-match
-                                          "\\([A-Z]+-[0-9]+\\)" branch))
-                                 (match-string 1 branch)))
-                         ;; Create a short summary from title (first 40 chars)
-                         (summary (when title
-                                    (let ((s (replace-regexp-in-string
-                                              "[^a-zA-Z0-9]+" "-"
-                                              (downcase (string-trim title)))))
-                                      (if (> (length s) 40)
-                                          (substring s 0 40)
-                                        s)))))
-                    (list (cons 'title title)
-                          (cons 'author author)
-                          (cons 'branch branch)
-                          (cons 'jira jira)
-                          (cons 'summary summary)))))
-            (error nil)))
-
         (defun decknix--agent-clipboard-url ()
           "Return a GitHub PR URL from the kill ring or system clipboard, or nil."
           (let ((text (or (ignore-errors (current-kill 0 t))
@@ -61,42 +24,85 @@
             (when (and text (string-match-p "github\\.com/.*/pull/" text))
               (string-trim text))))
 
-        (defvar decknix--review-pending nil
-          "Pending review data: (BUF URL ATTEMPTS-LEFT).
-Used by the retry timer to auto-send the review command once the
-agent process is ready.")
+        (defvar decknix--quickaction-pending nil
+          "Pending quick-action data: (BUF COMMAND ATTEMPTS-LEFT).
+Used by the retry timer to auto-send a command once the agent
+process is ready.")
 
-        (defun decknix--agent-review-try-send ()
-          "Try to send the review command to the pending session.
+        (defun decknix--agent-quickaction-try-send ()
+          "Try to send the pending command to the session buffer.
 Retries if the process is not yet ready."
-          (when decknix--review-pending
-            (let ((buf (nth 0 decknix--review-pending))
-                  (url (nth 1 decknix--review-pending))
-                  (attempts-left (nth 2 decknix--review-pending)))
+          (when decknix--quickaction-pending
+            (let ((buf (nth 0 decknix--quickaction-pending))
+                  (command (nth 1 decknix--quickaction-pending))
+                  (attempts-left (nth 2 decknix--quickaction-pending)))
               (cond
                ((not (buffer-live-p buf))
-                (setq decknix--review-pending nil))
+                (setq decknix--quickaction-pending nil))
                ((and (get-buffer-process buf)
                      (process-live-p (get-buffer-process buf)))
-                ;; Process is ready — send the review command
+                ;; Process is ready — send the command
                 (with-current-buffer buf
                   (goto-char (point-max))
-                  (shell-maker-submit
-                   :input (format "/review-service-pr %s" url)))
-                (setq decknix--review-pending nil)
-                (message "Review started for %s" url))
+                  (shell-maker-submit :input command))
+                (setq decknix--quickaction-pending nil)
+                (message "Sent: %s" (truncate-string-to-width command 60)))
                ((<= attempts-left 0)
-                (setq decknix--review-pending nil)
-                (message "Could not auto-send review: agent not ready"))
+                (setq decknix--quickaction-pending nil)
+                (message "Could not auto-send command: agent not ready"))
                (t
-                (setf (nth 2 decknix--review-pending) (1- attempts-left))
-                (run-at-time 2.0 nil #'decknix--agent-review-try-send))))))
+                (setf (nth 2 decknix--quickaction-pending) (1- attempts-left))
+                (run-at-time 2.0 nil #'decknix--agent-quickaction-try-send))))))
 
+        (defun decknix--agent-quickaction-start (name tags workspace command)
+          "Start a quick-action session with NAME, TAGS, WORKSPACE, and auto-send COMMAND.
+Creates a new agent session, applies metadata, then schedules sending
+COMMAND once the process is ready.  Returns immediately."
+          (let* ((workspace (expand-file-name workspace))
+                 (before-buffers (buffer-list))
+                 (augmented-cmd
+                  (append agent-shell-auggie-acp-command
+                          (list "--workspace-root" workspace)))
+                 (config
+                  (let ((base (agent-shell-auggie-make-agent-config)))
+                    (setf (alist-get :client-maker base)
+                          (eval `(lambda (buffer)
+                                   (agent-shell--make-acp-client
+                                    :command ,(car augmented-cmd)
+                                    :command-params ',(cdr augmented-cmd)
+                                    :environment-variables
+                                    (cond ((map-elt agent-shell-auggie-authentication :none)
+                                           agent-shell-auggie-environment)
+                                          ((map-elt agent-shell-auggie-authentication :login)
+                                           agent-shell-auggie-environment)
+                                          (t
+                                           (error "Invalid Auggie authentication")))
+                                    :context-buffer buffer)) t))
+                    base)))
+            (let ((default-directory workspace))
+              (agent-shell-start :config config))
+            (setq decknix--agent-session-cache-time 0)
+            (decknix--agent-session-new-post-create
+             before-buffers name tags workspace)
+            ;; Schedule auto-sending the command once the process is ready.
+            (let ((cmd command)
+                  (bufs before-buffers))
+              (run-at-time
+               3.0 nil
+               (eval
+                `(lambda ()
+                   (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
+                     (when shell-buf
+                       (setq decknix--quickaction-pending
+                             (list shell-buf ,cmd 5))
+                       (decknix--agent-quickaction-try-send))))
+                t)))))
+
         (defun decknix-agent-review-pr (url)
           "Start a PR review session for URL.
-Parses the GitHub PR URL, fetches metadata via gh CLI, creates a
-new session with auto-generated name and tags, then sends the
-/review-service-pr command.
+Parses the GitHub PR URL, creates a new session with auto-generated
+name and tags, then sends /review-service-pr.  Metadata enrichment
+\(author, Jira key, title\) is handled by the review command itself.
 
 Interactively, prompts for URL (defaulting to clipboard if it
 looks like a PR URL) and workspace (defaulting to current project)."
@@ -115,72 +121,19 @@
             (let* ((owner (alist-get 'owner parsed))
                    (repo (alist-get 'repo parsed))
                    (number (alist-get 'number parsed))
-                   ;; Fetch metadata (non-blocking-ish, ~200ms)
-                   (meta (progn
-                           (message "Fetching PR #%s metadata…" number)
-                           (decknix--agent-gh-pr-metadata owner repo number)))
-                   (title (or (alist-get 'title meta) ""))
-                   (author (alist-get 'author meta))
-                   (jira (alist-get 'jira meta))
-                   (summary (or (alist-get 'summary meta)
-                                (format "%s" number)))
-                   ;; Auto-generate session name: pr-<repo>-<number>-<summary>
-                   (name (format "pr-%s-%s-%s" repo number
-                                 (if (> (length summary) 30)
-                                     (substring summary 0 30)
-                                   summary)))
-                   ;; Auto-generate tags
-                   (tags (delq nil (list "review" repo author jira)))
+                   ;; Auto-generate session name: pr-<repo>-<number>
+                   (name (format "pr-%s-%s" repo number))
+                   ;; Tags from URL only — command can enrich later
+                   (tags (list "review" repo))
                    ;; Workspace: default to current project root
                    (default-ws (decknix--agent-detect-workspace))
                    (workspace (read-directory-name
                                (format "Workspace for %s/%s#%s: "
                                        owner repo number)
                                default-ws nil t))
-                   (workspace (expand-file-name workspace))
-                   ;; Confirm name and tags
+                   ;; Confirm name
                    (name (read-string (format "Session name [%s]: " name)
                                       nil nil name))
-                   (before-buffers (buffer-list))
-                   ;; Build augmented command with --workspace-root
-                   (augmented-cmd
-                    (append agent-shell-auggie-acp-command
-                            (list "--workspace-root" workspace)))
-                   (config
-                    (let ((base (agent-shell-auggie-make-agent-config)))
-                      (setf (alist-get :client-maker base)
-                            (eval `(lambda (buffer)
-                                     (agent-shell--make-acp-client
-                                      :command ,(car augmented-cmd)
-                                      :command-params ',(cdr augmented-cmd)
-                                      :environment-variables
-                                      (cond ((map-elt agent-shell-auggie-authentication :none)
-                                             agent-shell-auggie-environment)
-                                            ((map-elt agent-shell-auggie-authentication :login)
-                                             agent-shell-auggie-environment)
-                                            (t
-                                             (error "Invalid Auggie authentication")))
-                                      :context-buffer buffer)) t))
-                      base)))
-              ;; Start the session
-              (let ((default-directory workspace))
-                (agent-shell-start :config config))
-              (setq decknix--agent-session-cache-time 0)
-              ;; Post-creation: rename, tags, workspace, then auto-send review
-              (decknix--agent-session-new-post-create
-               before-buffers name tags workspace)
-              ;; Schedule auto-sending the review command once the process is ready.
-              ;; We need to find the new buffer first (same pattern as post-create).
-              (let ((pr-url url)
-                    (bufs before-buffers))
-                (run-at-time
-                 3.0 nil
-                 (eval
-                  `(lambda ()
-                     (let ((shell-buf (decknix--agent-find-new-shell-buffer ',bufs)))
-                       (when shell-buf
-                         (setq decknix--review-pending
-                               (list shell-buf ,pr-url 5))
-                         (decknix--agent-review-try-send))))
-                  t)))
-              (message "Starting review: %s/%s#%s — %s" owner repo number title))))
\ No newline at end of file
+                   (command (format "/review-service-pr %s" url)))
+              (decknix--agent-quickaction-start name tags workspace command)
+              (message "Starting review: %s/%s#%s" owner repo number))))
\ No newline at end of file
```

## Agent (2026-03-26 13:15:33)

Good — much cleaner. The `decknix--agent-quickaction-start` is now a reusable function that any quick action can call. Now let me build:


### Tool Call [completed]: Run `cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure...`

**Tool:** execute
**Timestamp:** 2026-03-26 13:16:14
**Command:** cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure --override-input decknix path:$HOME/tools/decknix 2>&1 | tail -10
**Parameters:**
wait: true
max_wait_seconds: 180
cwd: /Users/ldeck

```

```

## Agent (2026-03-26 13:16:19)

Build succeeded. Now let me commit:


### Tool Call [completed]: Run `cd ~/tools/decknix && git add -A && git diff --cached --stat`

**Tool:** execute
**Timestamp:** 2026-03-26 13:16:21
**Command:** cd ~/tools/decknix && git add -A && git diff --cached --stat
**Parameters:**
wait: true
max_wait_seconds: 5
cwd: /Users/ldeck/tools/decknix

```

```


### Tool Call [completed]: Run `cd ~/tools/decknix && git commit -m 'feat(agent-shell): add PR review quick acti...`

**Tool:** execute
**Timestamp:** 2026-03-26 13:16:40
**Command:** cd ~/tools/decknix && git commit -m 'feat(agent-shell): add PR review quick action (C-c A c r)

New quick-action system under C-c A c prefix:
- C-c A c r: Review PR — prompts for GitHub PR URL (defaults to
  clipboard), workspace, and session name. Parses owner/repo/number
  from URL, auto-generates name (pr-<repo>-<number>) and tags
  (review, <repo>), then creates a session and auto-sends
  /review-service-pr <url>.

Architecture:
- decknix--agent-quickaction-start: reusable function for any quick
  action — creates session with name/tags/workspace and auto-sends
  a command via retry timer once the process is ready.
- decknix--agent-parse-pr-url: lightweight URL-only parsing (no gh
  CLI dependency). Metadata enrichment deferred to the review command.
- decknix--agent-clipboard-url: checks kill-ring and pbpaste for PR URLs.

Also added which-key labels for all C-c A c sub-keys (r, c, n, e)
and updated the keybinding help reference.' && git push origin main 2>&1
**Parameters:**
wait: true
max_wait_seconds: 30
cwd: /Users/ldeck/tools/decknix

```

```


### Tool Call [completed]: Update 2 tasks

**Tool:** edit
**Timestamp:** 2026-03-26 13:16:48
**Parameters:**
tasks: [((task_id . "x293ZaG1WqhJJkL24T8MfN") (state . "COMPLETE")) ((task_id . "uDKFVJArj4d3Gus1DeRpjq") (state . "COMPLETE"))]

```
[x] Implement PR review quick action (C-c A c r)

[x] Simplify PR review to URL-only parsing (no gh CLI)
```

## Agent (2026-03-26 13:16:53)

Phase 1 done. Now let me move to the batch compose editor. Let me check the remaining tasks:


### Tool Call [completed]: View task list

**Tool:** read
**Timestamp:** 2026-03-26 13:16:54

```

```


### Tool Call [completed]: Update 1 task

**Tool:** edit
**Timestamp:** 2026-03-26 13:17:02
**Parameters:**
tasks: [((task_id . "gkQCwkFyKkGzibBFHwGphM") (state . "IN_PROGRESS"))]

```
[/] Implement batch compose editor (C-c A c B)
```

## Agent (2026-03-26 13:17:08)

Let me look at the existing compose buffer to understand the pattern, then implement the batch editor:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 2270-2340)

**Tool:** read
**Timestamp:** 2026-03-26 13:17:10
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [2270 2340]

```

```

## Agent (2026-03-26 13:17:14)

Now I have the compose pattern. Let me implement the batch editor. I'll add it right after the `decknix-agent-review-pr` function and before the `C-c A c` keybinding block:


### Tool Call [completed]: Read `modules/home/options/editors/emacs/agent-shell.nix` (lines 2555-2575)

**Tool:** read
**Timestamp:** 2026-03-26 13:17:17
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
view_range: [2555 2575]

```

```


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:18:13
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:         ;; C-c A c — commands sub-prefix ("Commands")
        (define-prefix-command 'decknix-agent-command-map)
        (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
        (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
        (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
new_str_1:         ;; == Batch processing: launch multiple sessions from a compose editor ==
        ;; Syntax:
        ;;   --- <group-name> [: <workspace>]
        ;;   <url-or-item>
        ;;   <url-or-item>
        ;;
        ;;   --- <another-group> [: <workspace>]
        ;;   <url-or-item>
        ;;
        ;;   <ungrouped-url>          ← gets its own session
        ;;
        ;; Lines within a group share a single session.
        ;; Ungrouped lines each get their own session.
        ;; Default workspace is the current project root.

        (defvar decknix--batch-default-workspace nil
          "Default workspace for the current batch editor.")

        (defun decknix--batch-parse-buffer ()
          "Parse the batch editor buffer into a list of session specs.
Each spec is an alist with keys: name, workspace, items, grouped."
          (let ((specs nil)
                (current-group nil)
                (current-items nil)
                (current-ws decknix--batch-default-workspace)
                (current-name nil))
            (save-excursion
              (goto-char (point-min))
              (while (not (eobp))
                (let ((line (string-trim
                             (buffer-substring-no-properties
                              (line-beginning-position)
                              (line-end-position)))))
                  (cond
                   ;; Divider: --- <name> [: <workspace>]
                   ((string-match "^---\\s-+\\(.+\\)" line)
                    ;; Flush previous group if any
                    (when (and current-name current-items)
                      (push (list (cons 'name current-name)
                                  (cons 'workspace current-ws)
                                  (cons 'items (nreverse current-items))
                                  (cons 'grouped t))
                            specs))
                    ;; Parse new group header
                    (let ((header (match-string 1 line)))
                      (if (string-match "^\\(.+?\\)\\s-*:\\s-*\\(\\S-+.*\\)" header)
                          (progn
                            (setq current-name (string-trim (match-string 1 header)))
                            (setq current-ws (expand-file-name
                                              (string-trim (match-string 2 header)))))
                        (setq current-name (string-trim header))
                        (setq current-ws decknix--batch-default-workspace)))
                    (setq current-items nil))
                   ;; Empty line or comment — skip
                   ((or (string-empty-p line)
                        (string-prefix-p "#" line))
                    nil)
                   ;; Content line
                   (t
                    (if current-name
                        ;; Inside a group
                        (push line current-items)
                      ;; Ungrouped — each line is its own session
                      (let* ((parsed (decknix--agent-parse-pr-url line))
                             (auto-name (if parsed
                                            (format "pr-%s-%s"
                                                    (alist-get 'repo parsed)
                                                    (alist-get 'number parsed))
                                          (format "review-%s"
                                                  (substring
                                                   (secure-hash 'sha256 line)
                                                   0 8)))))
                        (push (list (cons 'name auto-name)
                                    (cons 'workspace
                                          decknix--batch-default-workspace)
                                    (cons 'items (list line))
                                    (cons 'grouped nil))
                              specs))))))
                (forward-line 1)))
            ;; Flush final group
            (when (and current-name current-items)
              (push (list (cons 'name current-name)
                          (cons 'workspace current-ws)
                          (cons 'items (nreverse current-items))
                          (cons 'grouped t))
                    specs))
            (nreverse specs)))

        (defvar decknix--batch-launch-results nil
          "List of (NAME STATUS BUFFER) for the most recent batch launch.")

        (defun decknix--batch-launch (specs)
          "Launch sessions for each spec in SPECS.
Each spec is an alist with name, workspace, items, grouped.
Grouped specs send all items as a single message.
Ungrouped specs send each item via /review-service-pr."
          (setq decknix--batch-launch-results nil)
          (dolist (spec specs)
            (let* ((name (alist-get 'name spec))
                   (workspace (alist-get 'workspace spec))
                   (items (alist-get 'items spec))
                   (grouped (alist-get 'grouped spec))
                   ;; Build the command to send
                   (command (if grouped
                                ;; Grouped: send all items as one message
                                (mapconcat
                                 (lambda (item)
                                   (format "/review-service-pr %s" item))
                                 items "\n")
                              ;; Ungrouped: single item
                              (format "/review-service-pr %s" (car items))))
                   ;; Tags: review + repo names from parsed URLs
                   (tags (let ((tag-list (list "review")))
                           (dolist (item items)
                             (let ((parsed (decknix--agent-parse-pr-url item)))
                               (when parsed
                                 (cl-pushnew (alist-get 'repo parsed)
                                             tag-list :test #'string=))))
                           tag-list)))
              (condition-case err
                  (progn
                    (decknix--agent-quickaction-start name tags workspace command)
                    (push (list name "launched" nil) decknix--batch-launch-results))
                (error
                 (push (list name "failed" (error-message-string err))
                       decknix--batch-launch-results)))))
          (setq decknix--batch-launch-results
                (nreverse decknix--batch-launch-results))
          ;; Show summary
          (decknix--batch-show-summary))

        (defun decknix--batch-show-summary ()
          "Display a summary buffer of the batch launch results."
          (let ((buf (get-buffer-create "*Batch Launch*")))
            (with-current-buffer buf
              (let ((inhibit-read-only t))
                (erase-buffer)
                (insert (propertize "Batch Launch Summary\n"
                                    'font-lock-face '(:weight bold :height 1.2)))
                (insert (propertize (make-string 40 ?═)
                                    'font-lock-face 'font-lock-comment-face)
                        "\n\n")
                (dolist (result decknix--batch-launch-results)
                  (let ((name (nth 0 result))
                        (status (nth 1 result))
                        (err (nth 2 result)))
                    (insert (propertize
                             (if (string= status "launched") "✓ " "✗ ")
                             'font-lock-face
                             (if (string= status "launched")
                                 'success 'error))
                            (propertize name 'font-lock-face '(:weight bold))
                            (format "  — %s" status)
                            (if err (format " (%s)" err) "")
                            "\n")))
                (insert "\n"
                        (propertize (format "%d sessions launched"
                                           (length decknix--batch-launch-results))
                                    'font-lock-face 'font-lock-comment-face)
                        "\n\n"
                        (propertize "Press q to close.\n"
                                    'font-lock-face 'font-lock-comment-face))
                (special-mode)
                (goto-char (point-min))))
            (display-buffer buf
                            '((display-buffer-at-bottom)
                              (window-height . fit-window-to-buffer)))))

        ;; -- Batch compose minor mode for syntax highlighting --

        (defvar decknix-batch-compose-mode-map
          (let ((map (make-sparse-keymap)))
            (define-key map (kbd "C-c C-c") 'decknix--batch-submit)
            (define-key map (kbd "C-c C-k") 'decknix--batch-cancel)
            map)
          "Keymap for `decknix-batch-compose-mode'.")

        (defun decknix--batch-submit ()
          "Parse and launch all sessions from the batch editor."
          (interactive)
          (let ((specs (decknix--batch-parse-buffer)))
            (if (null specs)
                (user-error "No items to process — add URLs or groups")
              (when (y-or-n-p (format "Launch %d session(s)? "
                                      (length specs)))
                (let ((buf (current-buffer)))
                  (decknix--batch-launch specs)
                  (when (buffer-live-p buf)
                    (kill-buffer buf)))))))

        (defun decknix--batch-cancel ()
          "Cancel the batch editor."
          (interactive)
          (when (y-or-n-p "Cancel batch? ")
            (kill-buffer (current-buffer))))

        (defvar decknix--batch-font-lock-keywords
          (list
           ;; --- divider lines (group headers)
           (list "^---\\s-+\\(.+?\\)\\(\\s-*:\\s-*\\(\\S-+.*\\)\\)?$"
                 '(0 'font-lock-keyword-face t)
                 '(1 'font-lock-function-name-face t)
                 '(3 'font-lock-string-face t t))
           ;; GitHub PR URLs
           (list "https?://github\\.com/[^ \t\n]+"
                 '(0 'link t))
           ;; Comments
           (list "^#.*$"
                 '(0 'font-lock-comment-face t)))
          "Font-lock keywords for batch compose mode.")

        (define-minor-mode decknix-batch-compose-mode
          "Minor mode for the batch session editor.
Provides syntax highlighting for --- dividers, URLs, and comments.
\\<decknix-batch-compose-mode-map>
\\[decknix--batch-submit]  Submit — parse and launch all sessions.
\\[decknix--batch-cancel]  Cancel — close without launching."
          :lighter " Batch"
          :keymap decknix-batch-compose-mode-map
          (if decknix-batch-compose-mode
              (progn
                (font-lock-add-keywords nil decknix--batch-font-lock-keywords)
                (setq-local header-line-format
                            (list
                             (propertize
                              " Batch: C-c C-c submit | C-c C-k cancel"
                              'face 'header-line)))
                (font-lock-flush))
            (font-lock-remove-keywords nil decknix--batch-font-lock-keywords)
            (setq-local header-line-format nil)
            (font-lock-flush)))

        (defun decknix-agent-batch-process ()
          "Open a batch compose editor for launching multiple sessions.
Syntax:
  --- <group-name> [: <workspace>]
  <url>
  <url>

  --- <another-group> [: ~/other/path]
  <url>

  <ungrouped-url>

Lines within a --- group share a single session.
Ungrouped lines each get their own session.
Comments start with #."
          (interactive)
          (let* ((default-ws (decknix--agent-detect-workspace))
                 (buf (generate-new-buffer "*Batch Process*")))
            (display-buffer buf
                            '((display-buffer-at-bottom)
                              (window-height . 15)
                              (dedicated . t)))
            (select-window (get-buffer-window buf))
            (with-current-buffer buf
              (text-mode)
              (setq-local decknix--batch-default-workspace default-ws)
              (decknix-batch-compose-mode 1)
              ;; Insert template
              (insert (format "# Batch session launcher — workspace: %s\n"
                              default-ws)
                      "# Syntax: --- <name> [: <workspace>]\n"
                      "#         <url-per-line>\n"
                      "# Ungrouped URLs get individual sessions.\n"
                      "# C-c C-c to launch, C-c C-k to cancel.\n\n")
              (set-buffer-modified-p nil))))

        ;; C-c A c — commands sub-prefix ("Commands")
        (define-prefix-command 'decknix-agent-command-map)
        (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
        (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
        (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
        (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
        (define-key decknix-agent-command-map (kbd "B") 'decknix-agent-batch-process)  ; Batch
old_str_start_line_number_1: 2566
old_str_end_line_number_1: 2572

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1,7 +1,276 @@
+        ;; == Batch processing: launch multiple sessions from a compose editor ==
+        ;; Syntax:
+        ;;   --- <group-name> [: <workspace>]
+        ;;   <url-or-item>
+        ;;   <url-or-item>
+        ;;
+        ;;   --- <another-group> [: <workspace>]
+        ;;   <url-or-item>
+        ;;
+        ;;   <ungrouped-url>          ← gets its own session
+        ;;
+        ;; Lines within a group share a single session.
+        ;; Ungrouped lines each get their own session.
+        ;; Default workspace is the current project root.
+
+        (defvar decknix--batch-default-workspace nil
+          "Default workspace for the current batch editor.")
+
+        (defun decknix--batch-parse-buffer ()
+          "Parse the batch editor buffer into a list of session specs.
+Each spec is an alist with keys: name, workspace, items, grouped."
+          (let ((specs nil)
+                (current-group nil)
+                (current-items nil)
+                (current-ws decknix--batch-default-workspace)
+                (current-name nil))
+            (save-excursion
+              (goto-char (point-min))
+              (while (not (eobp))
+                (let ((line (string-trim
+                             (buffer-substring-no-properties
+                              (line-beginning-position)
+                              (line-end-position)))))
+                  (cond
+                   ;; Divider: --- <name> [: <workspace>]
+                   ((string-match "^---\\s-+\\(.+\\)" line)
+                    ;; Flush previous group if any
+                    (when (and current-name current-items)
+                      (push (list (cons 'name current-name)
+                                  (cons 'workspace current-ws)
+                                  (cons 'items (nreverse current-items))
+                                  (cons 'grouped t))
+                            specs))
+                    ;; Parse new group header
+                    (let ((header (match-string 1 line)))
+                      (if (string-match "^\\(.+?\\)\\s-*:\\s-*\\(\\S-+.*\\)" header)
+                          (progn
+                            (setq current-name (string-trim (match-string 1 header)))
+                            (setq current-ws (expand-file-name
+                                              (string-trim (match-string 2 header)))))
+                        (setq current-name (string-trim header))
+                        (setq current-ws decknix--batch-default-workspace)))
+                    (setq current-items nil))
+                   ;; Empty line or comment — skip
+                   ((or (string-empty-p line)
+                        (string-prefix-p "#" line))
+                    nil)
+                   ;; Content line
+                   (t
+                    (if current-name
+                        ;; Inside a group
+                        (push line current-items)
+                      ;; Ungrouped — each line is its own session
+                      (let* ((parsed (decknix--agent-parse-pr-url line))
+                             (auto-name (if parsed
+                                            (format "pr-%s-%s"
+                                                    (alist-get 'repo parsed)
+                                                    (alist-get 'number parsed))
+                                          (format "review-%s"
+                                                  (substring
+                                                   (secure-hash 'sha256 line)
+                                                   0 8)))))
+                        (push (list (cons 'name auto-name)
+                                    (cons 'workspace
+                                          decknix--batch-default-workspace)
+                                    (cons 'items (list line))
+                                    (cons 'grouped nil))
+                              specs))))))
+                (forward-line 1)))
+            ;; Flush final group
+            (when (and current-name current-items)
+              (push (list (cons 'name current-name)
+                          (cons 'workspace current-ws)
+                          (cons 'items (nreverse current-items))
+                          (cons 'grouped t))
+                    specs))
+            (nreverse specs)))
+
+        (defvar decknix--batch-launch-results nil
+          "List of (NAME STATUS BUFFER) for the most recent batch launch.")
+
+        (defun decknix--batch-launch (specs)
+          "Launch sessions for each spec in SPECS.
+Each spec is an alist with name, workspace, items, grouped.
+Grouped specs send all items as a single message.
+Ungrouped specs send each item via /review-service-pr."
+          (setq decknix--batch-launch-results nil)
+          (dolist (spec specs)
+            (let* ((name (alist-get 'name spec))
+                   (workspace (alist-get 'workspace spec))
+                   (items (alist-get 'items spec))
+                   (grouped (alist-get 'grouped spec))
+                   ;; Build the command to send
+                   (command (if grouped
+                                ;; Grouped: send all items as one message
+                                (mapconcat
+                                 (lambda (item)
+                                   (format "/review-service-pr %s" item))
+                                 items "\n")
+                              ;; Ungrouped: single item
+                              (format "/review-service-pr %s" (car items))))
+                   ;; Tags: review + repo names from parsed URLs
+                   (tags (let ((tag-list (list "review")))
+                           (dolist (item items)
+                             (let ((parsed (decknix--agent-parse-pr-url item)))
+                               (when parsed
+                                 (cl-pushnew (alist-get 'repo parsed)
+                                             tag-list :test #'string=))))
+                           tag-list)))
+              (condition-case err
+                  (progn
+                    (decknix--agent-quickaction-start name tags workspace command)
+                    (push (list name "launched" nil) decknix--batch-launch-results))
+                (error
+                 (push (list name "failed" (error-message-string err))
+                       decknix--batch-launch-results)))))
+          (setq decknix--batch-launch-results
+                (nreverse decknix--batch-launch-results))
+          ;; Show summary
+          (decknix--batch-show-summary))
+
+        (defun decknix--batch-show-summary ()
+          "Display a summary buffer of the batch launch results."
+          (let ((buf (get-buffer-create "*Batch Launch*")))
+            (with-current-buffer buf
+              (let ((inhibit-read-only t))
+                (erase-buffer)
+                (insert (propertize "Batch Launch Summary\n"
+                                    'font-lock-face '(:weight bold :height 1.2)))
+                (insert (propertize (make-string 40 ?═)
+                                    'font-lock-face 'font-lock-comment-face)
+                        "\n\n")
+                (dolist (result decknix--batch-launch-results)
+                  (let ((name (nth 0 result))
+                        (status (nth 1 result))
+                        (err (nth 2 result)))
+                    (insert (propertize
+                             (if (string= status "launched") "✓ " "✗ ")
+                             'font-lock-face
+                             (if (string= status "launched")
+                                 'success 'error))
+                            (propertize name 'font-lock-face '(:weight bold))
+                            (format "  — %s" status)
+                            (if err (format " (%s)" err) "")
+                            "\n")))
+                (insert "\n"
+                        (propertize (format "%d sessions launched"
+                                           (length decknix--batch-launch-results))
+                                    'font-lock-face 'font-lock-comment-face)
+                        "\n\n"
+                        (propertize "Press q to close.\n"
+                                    'font-lock-face 'font-lock-comment-face))
+                (special-mode)
+                (goto-char (point-min))))
+            (display-buffer buf
+                            '((display-buffer-at-bottom)
+                              (window-height . fit-window-to-buffer)))))
+
+        ;; -- Batch compose minor mode for syntax highlighting --
+
+        (defvar decknix-batch-compose-mode-map
+          (let ((map (make-sparse-keymap)))
+            (define-key map (kbd "C-c C-c") 'decknix--batch-submit)
+            (define-key map (kbd "C-c C-k") 'decknix--batch-cancel)
+            map)
+          "Keymap for `decknix-batch-compose-mode'.")
+
+        (defun decknix--batch-submit ()
+          "Parse and launch all sessions from the batch editor."
+          (interactive)
+          (let ((specs (decknix--batch-parse-buffer)))
+            (if (null specs)
+                (user-error "No items to process — add URLs or groups")
+              (when (y-or-n-p (format "Launch %d session(s)? "
+                                      (length specs)))
+                (let ((buf (current-buffer)))
+                  (decknix--batch-launch specs)
+                  (when (buffer-live-p buf)
+                    (kill-buffer buf)))))))
+
+        (defun decknix--batch-cancel ()
+          "Cancel the batch editor."
+          (interactive)
+          (when (y-or-n-p "Cancel batch? ")
+            (kill-buffer (current-buffer))))
+
+        (defvar decknix--batch-font-lock-keywords
+          (list
+           ;; --- divider lines (group headers)
+           (list "^---\\s-+\\(.+?\\)\\(\\s-*:\\s-*\\(\\S-+.*\\)\\)?$"
+                 '(0 'font-lock-keyword-face t)
+                 '(1 'font-lock-function-name-face t)
+                 '(3 'font-lock-string-face t t))
+           ;; GitHub PR URLs
+           (list "https?://github\\.com/[^ \t\n]+"
+                 '(0 'link t))
+           ;; Comments
+           (list "^#.*$"
+                 '(0 'font-lock-comment-face t)))
+          "Font-lock keywords for batch compose mode.")
+
+        (define-minor-mode decknix-batch-compose-mode
+          "Minor mode for the batch session editor.
+Provides syntax highlighting for --- dividers, URLs, and comments.
+\\<decknix-batch-compose-mode-map>
+\\[decknix--batch-submit]  Submit — parse and launch all sessions.
+\\[decknix--batch-cancel]  Cancel — close without launching."
+          :lighter " Batch"
+          :keymap decknix-batch-compose-mode-map
+          (if decknix-batch-compose-mode
+              (progn
+                (font-lock-add-keywords nil decknix--batch-font-lock-keywords)
+                (setq-local header-line-format
+                            (list
+                             (propertize
+                              " Batch: C-c C-c submit | C-c C-k cancel"
+                              'face 'header-line)))
+                (font-lock-flush))
+            (font-lock-remove-keywords nil decknix--batch-font-lock-keywords)
+            (setq-local header-line-format nil)
+            (font-lock-flush)))
+
+        (defun decknix-agent-batch-process ()
+          "Open a batch compose editor for launching multiple sessions.
+Syntax:
+  --- <group-name> [: <workspace>]
+  <url>
+  <url>
+
+  --- <another-group> [: ~/other/path]
+  <url>
+
+  <ungrouped-url>
+
+Lines within a --- group share a single session.
+Ungrouped lines each get their own session.
+Comments start with #."
+          (interactive)
+          (let* ((default-ws (decknix--agent-detect-workspace))
+                 (buf (generate-new-buffer "*Batch Process*")))
+            (display-buffer buf
+                            '((display-buffer-at-bottom)
+                              (window-height . 15)
+                              (dedicated . t)))
+            (select-window (get-buffer-window buf))
+            (with-current-buffer buf
+              (text-mode)
+              (setq-local decknix--batch-default-workspace default-ws)
+              (decknix-batch-compose-mode 1)
+              ;; Insert template
+              (insert (format "# Batch session launcher — workspace: %s\n"
+                              default-ws)
+                      "# Syntax: --- <name> [: <workspace>]\n"
+                      "#         <url-per-line>\n"
+                      "# Ungrouped URLs get individual sessions.\n"
+                      "# C-c C-c to launch, C-c C-k to cancel.\n\n")
+              (set-buffer-modified-p nil))))
+
         ;; C-c A c — commands sub-prefix ("Commands")
         (define-prefix-command 'decknix-agent-command-map)
         (define-key decknix-agent-prefix-map (kbd "c") 'decknix-agent-command-map)
         (define-key decknix-agent-command-map (kbd "c") 'decknix-agent-command-run)    ; Pick & insert
         (define-key decknix-agent-command-map (kbd "n") 'decknix-agent-command-new)    ; New
         (define-key decknix-agent-command-map (kbd "e") 'decknix-agent-command-edit)   ; Edit
-        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
\ No newline at end of file
+        (define-key decknix-agent-command-map (kbd "r") 'decknix-agent-review-pr)      ; PR review
+        (define-key decknix-agent-command-map (kbd "B") 'decknix-agent-batch-process)  ; Batch
\ No newline at end of file
```

## Agent (2026-03-26 13:18:19)

Now add the which-key label for `B`:


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:18:24
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:             "C-c A c r" "review PR"
            "C-c A c c" "run command"
            "C-c A c n" "new command"
            "C-c A c e" "edit command"
new_str_1:             "C-c A c r" "review PR"
            "C-c A c B" "batch process"
            "C-c A c c" "run command"
            "C-c A c n" "new command"
            "C-c A c e" "edit command"
old_str_start_line_number_1: 625
old_str_end_line_number_1: 628

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1,4 +1,5 @@
             "C-c A c r" "review PR"
+            "C-c A c B" "batch process"
             "C-c A c c" "run command"
             "C-c A c n" "new command"
             "C-c A c e" "edit command"
\ No newline at end of file
```

## Agent (2026-03-26 13:18:29)

Also update the help reference:


### Tool Call [completed]: Edit modules/home/options/editors/emacs/agent-shell.nix

**Tool:** edit
**Timestamp:** 2026-03-26 13:18:32
**Command:** str_replace
**Parameters:**
path: modules/home/options/editors/emacs/agent-shell.nix
instruction_reminder: ALWAYS BREAK DOWN EDITS INTO SMALLER CHUNKS OF AT MOST 150 LINES EACH.
old_str_1:             "  C-c A c r   Review PR (quick action)\n"
new_str_1:             "  C-c A c r   Review PR (quick action)\n"
            "  C-c A c B   Batch process (multi-session)\n"
old_str_start_line_number_1: 479
old_str_end_line_number_1: 479

```
╭─────────╮
│ changes │
╰─────────╯

@@ -1 +1,2 @@
-            "  C-c A c r   Review PR (quick action)\n"
\ No newline at end of file
+            "  C-c A c r   Review PR (quick action)\n"
+            "  C-c A c B   Batch process (multi-session)\n"
\ No newline at end of file
```

## Agent (2026-03-26 13:18:37)

Now build:


### Tool Call [completed]: Run `cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure...`

**Tool:** execute
**Timestamp:** 2026-03-26 13:19:15
**Command:** cd ~/.config/decknix && nix build .#darwinConfigurations.default.system --impure --override-input decknix path:$HOME/tools/decknix 2>&1 | tail -10
**Parameters:**
wait: true
max_wait_seconds: 180
cwd: /Users/ldeck

```

```

## Agent (2026-03-26 13:19:20)

Build succeeded. Let me commit:
