{
  # USER IDENTITY
  username = "REPLACE_ME";       # Your macOS username (run `whoami`)
  role     = "developer";        # Your role ('developer', 'designer', 'tester', etc) 
  
  # MACHINE IDENTITY
  hostname = "deck-macbook";     # Your hostname (run `hostname -s`)
  system   = "aarch64-darwin";   # "aarch64-darwin" (Apple Silicon) or "x86_64-darwin" (Intel) and/or replace 'darwin' with 'linux'
}
